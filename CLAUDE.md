# samhyun.dev

Astro 기반 개인 블로그 · 포트폴리오 사이트.

## Agent skills

### Issue tracker

이슈와 스펙은 저장소 안 `.scratch/<feature>/` 마크다운 파일로 관리합니다 (git에는 커밋하지 않음). `docs/agents/issue-tracker.md` 참고.

### Triage labels

기본 다섯 개 역할 라벨(`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`)을 그대로 씁니다. `docs/agents/triage-labels.md` 참고.

### Domain docs

single-context — 루트 `CONTEXT.md` + `docs/adr/`. `docs/agents/domain.md` 참고.
